# idiogen

idioma in spanish is language so idiogen is kinda like language generator

language prompt -> bison/flex file + interpretter generator -> compile and return (for their target machine) (or just c file ig)

## how to use
